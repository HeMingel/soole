<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.songpo.searched.mapper.SpecificationNameMapper">
    <select id="findSpecificationContentBySpecificationId" resultType="java.lang.String">
        select ss.content from sl_specification AS ss
        LEFT JOIN sl_repository sr on ss.id = sr.specification_id
        LEFT JOIN sl_product sp ON sp.id = sr.product_id
        WHERE sr.product_id = #{arg1} and sr.count > 0 AND ss.id = #{arg0}
    </select>
</mapper>